import attachLocale from './attachLocale';
import attachCurrentUser from './attachCurrentUser';
import authCheck from './authCheck';
import authRequired from './authRequired';

export default {
    attachLocale,
    attachCurrentUser,
    authCheck,
    authRequired
};